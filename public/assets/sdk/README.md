# OAuth SDK 文件夹

此文件夹用于存放各语言的SDK压缩包供用户下载。

## 文件命名规范

- PHP SDK: `oauth-sdk-php.zip`
- JavaScript SDK: `oauth-sdk-js.zip`
- Python SDK: `oauth-sdk-python.zip`

## 使用说明

将对应语言的SDK压缩包放置在此目录下，用户即可通过接入文档页面下载。
